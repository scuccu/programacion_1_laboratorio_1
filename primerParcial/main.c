#include <stdio.h>
#include <stdlib.h>
#include "usuarios.h"
//#include "susFunciones.h"
#define CANTIDAD 10

int main()
{

    char seguir='s';
    int opcion=0;
    eUsuario

   do
   {
       printf("1. ALTA USUARIO\n2.");

       switch(menu)
       {
       case 1:
            eUsuario_alta(eUsuario  listado[],int limite)
            if(index == -1)
            {
                printf("No hay lugar");
            }
            else
            {
                printf("Alumno ingresado");
            }
            break;
        case 2:
       }
   }
}
